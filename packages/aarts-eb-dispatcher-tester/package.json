{
  "name": "aarts-eb-dispatcher-tester",
  "version": "1.0.14",
  "description": "In a loop it dispatches events to bus, decorating them with a client token.",
  "main": "./dist/eventDispatcherStressTester.js",
  "dependencies": {
    "@types/aws-lambda": "^8.10.59",
    "@types/node": "^13.13.14",
    "@types/request": "^2.48.4",
    "aarts-eb-types": "^0.2.26",
    "aws-sdk": "^2.713.0",
    "typescript": "^3.9.6"
  },
  "devDependencies": {},
  "scripts": {
    "build_win": "npm run clean_win && tsc && copy package.json .\\dist",
    "build_lin": "npm run clean_lin && tsc && cp package.json ./dist",
    "watch": "tsc -w",
    "clean_lin": "rm -fr ./dist",
    "clean_win": "rmdir /Q /S dist",
    "test": "jest"
  },
  "author": "akrsmv",
  "license": "ISC"
}
